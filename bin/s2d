#! /usr/bin/python3

"""Simply replace all slashes with dots."""

import sys


def run(args):
  assert args
  print(" ".join([arg.replace("/", ".") for arg in args]))


if __name__ == "__main__":
  run(args=sys.argv[1:])
